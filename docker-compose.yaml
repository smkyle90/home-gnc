version: '3'
services:
  controller:
    network_mode: host
    build:
      context: .
      dockerfile: ./Dockerfile.c
  guidance:
    network_mode: host
    build:
      context: .
      dockerfile: ./Dockerfile.g
